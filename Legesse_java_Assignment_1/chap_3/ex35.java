/* 
3.5 what is the purpose of keyword NEW ? explaian what happens when you use it 
-thd purpose "new"  keyword is to dynamically allocate memory for an object at runtime.
when you use it the memory is allocated and stored the object then the constractor is called initialize the object
*/